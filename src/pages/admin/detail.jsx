import withLayout from "@/layouts/withLayout";

const DetailPage = () => {
    return (
        <div>
        <h1>Detail Page</h1>
        </div>
    );
}

export default withLayout(DetailPage, 'admin');